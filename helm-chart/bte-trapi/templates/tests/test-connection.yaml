apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "biothings-explorer.fullname" . }}-test-connection"
  labels:
    {{- include "biothings-explorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "biothings-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
